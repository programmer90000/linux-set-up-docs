# Application Dashboard Configuration

1. Right-click on the application dashboard icon
2. Select `Edit Applications...`
3. Delete all existing menus
4. Create the following submenus:
- `Internet`
- `Image & Video`
- `Development`
- `LibreOffice`
- `Utilities`
- `Settings`
- `System`

---

## Menu Structure

### Internet

- Create a new item with the name `Brave`
    - `Name`: `Brave`
    - `Program`: `/usr/bin/brave-browser-stable`
    - `Icon`: `brave-browser`
    - Enable `Enable launch feedback`
    - Disable `Only show when logged into a Plasma session`

> Note: For the 3 Microsoft Edge profiles, set only the name and enable launch feedback. Do not set any other options

- Create a new item with the name `Microsoft Edge (Personal)`
    - `Name`: `Microsoft Edge (Personal)`
    - Enable `Enable launch feedback`

- Create a new item with the name `Microsoft Edge (Programming)`
    - `Name`: `Microsoft Edge (Programming)`
    - `Icon`: `microsoft-edge-logo-programming`
    - Enable `Enable launch feedback`

- Create a new item with the name `Microsoft Edge (Quran)`
    - `Name`: `Microsoft Edge (Quran)`
    - Enable `Enable launch feedback`

- Create a new item with the name `Google Chrome`
    - `Name`: `Google Chrome`
    - `Program`: `/bin/google-chrome-stable`
    - `Icon`: `google-chrome`
    - Enable `Enable launch feedback`
    - Disable `Only show when logged into a Plasma session`

- Create a new item with the name `Thunderbird`
    - `Name`: `Thunderbird`
    - `Program`: `/usr/bin/thunderbird`
    - `Icon`: `thunderbird`
    - Enable `Enable launch feedback`
    - Disable `Only show when logged into a Plasma session`

### Image & Video

- Create a new item with the name `HandBrake`
    - `Name`: `HandBrake`
    - `Program`: `/usr/bin/handbrake`
    - `Icon`: `fr.handbrake.ghb`
    - Enable `Enable launch feedback`
    - Disable `Only show when logged into a Plasma session`

- Create a new item with the name `VLC Media Player`
    - `Name`: `VLC Media Player`
    - `Program`: `/usr/bin/vlc`
    - `Icon`: `vlc`
    - Enable `Enable launch feedback`
    - Disable `Only show when logged into a Plasma session`

- Create a new item with the name `Viewnior Image Viewer`
    - `Name`: `Viewnior Image Viewer`
    - `Program`: `/usr/bin/viewnior`
    - `Icon`: `viewnior`
    - Enable `Enable launch feedback`
    - Disable `Only show when logged into a Plasma session`

- Create a new item with the name `Gimp Image Editor`
    - `Name`: `Gimp Image Editor`
    - `Program`: `/usr/bin/gimp`
    - `Icon`: `gimp`
    - Enable `Enable launch feedback`
    - Disable `Only show when logged into a Plasma session`

- Create a new item with the name `KDenLive`
    - `Name`: `KDenLive`
    - `Program`: `/usr/bin/kdenlive`
    - `Icon`: `kdenlive`
    - Enable `Enable launch feedback`
    - Disable `Only show when logged into a Plasma session`

### Development

- Create a new item with the name `Konsole`
    - `Name`: `Konsole`
    - `Program`: `konsole`
    - `Icon`: `utilities-terminal`
    - Enable `Enable launch feedback`
    - Disable `Only show when logged into a Plasma session`

- Create a new item with the name `FileZilla`
    - `Name`: `FileZilla`
    - `Program`: `/usr/bin/filezilla`
    - `Icon`: `filezilla`
    - Enable `Enable launch feedback`
    - Disable `Only show when logged into a Plasma session`

- Create a new item with the name `Mousepad Text Editor`
    - `Name`: `Mousepad Text Editor`
    - `Program`: `/usr/bin/mousepad`
    - `Icon`: `org.xfce.mousepad`
    - Enable `Enable launch feedback`
    - Disable `Only show when logged into a Plasma session`

- Create a new item with the name `GitHub Desktop`
    - `Name`: `GitHub Desktop`
    - `Program`: `/usr/bin/github-desktop`
    - `Icon`: `github-desktop`
    - Enable `Enable launch feedback`
    - Disable `Only show when logged into a Plasma session`

- Create a new item with the name `Android Studio`
    - `Name`: `Android Studio`
    - `Program`: `/opt/android-studio/bin/studio.sh`
    - `Icon`: `android-studio`
    - Enable `Enable launch feedback`
    - Disable `Only show when logged into a Plasma session`

- Create a new item with the name `Docker Desktop`
    - `Name`: `Docker Desktop`
    - `Program`: `/opt/docker-desktop/bin/docker-desktop`
    - `Icon`: `docker-desktop`
    - Enable `Enable launch feedback`
    - Disable `Only show when logged into a Plasma session`

- Create a new item with the name `Visual Studio Code`
    - `Name`: `Visual Studio Code`
    - `Program`: `/usr/bin/code`
    - `Icon`: `visual-studio-code`
    - Enable `Enable launch feedback`
    - Disable `Only show when logged into a Plasma session`

- Create a new item with the name `KDE Connect`
    - `Name`: `KDE Connect`
    - `Program`: `kdeconnect-app`
    - `Icon`: `prefrences-kde-connect`
    - Enable `Enable launch feedback`
    - Disable `Only show when logged into a Plasma session`

### LibreOffice

- Create a new item with the name `LibreOffice StartCenter`
    - `Name`: `LibreOffice StartCenter`
    - `Program`: `LibreOffice StartCenter`
    - `Icon`: `libreoffice-startcenter`
    - Enable `Enable launch feedback`
    - Disable `Only show when logged into a Plasma session`

- Create a new item with the name `LibreOffice Writer`
    - `Name`: `LibreOffice Writer`
    - `Program`: `lowriter`
    - `Icon`: `libreoffice-writer`
    - Enable `Enable launch feedback`
    - Disable `Only show when logged into a Plasma session`

- Create a new item with the name `LibreOffice Calc`
    - `Name`: `LibreOffice Calc`
    - `Program`: `localc`
    - `Icon`: `libreoffice-calc`
    - Enable `Enable launch feedback`
    - Disable `Only show when logged into a Plasma session`

- Create a new item with the name `LibreOffice Impress`
    - `Name`: `LibreOffice Impress`
    - `Program`: `loimpress`
    - `Icon`: `libreoffice-impress`
    - Enable `Enable launch feedback`
    - Disable `Only show when logged into a Plasma session`

- Create a new item with the name `LibreOffice Draw`
    - `Name`: `LibreOffice Draw`
    - `Program`: `lodraw`
    - `Icon`: `libreoffice-draw`
    - Enable `Enable launch feedback`
    - Disable `Only show when logged into a Plasma session`

- Create a new item with the name `LibreOffice Math`
    - `Name`: `LibreOffice Math`
    - `Program`: `lomath`
    - `Icon`: `libreoffice-math`
    - Enable `Enable launch feedback`
    - Disable `Only show when logged into a Plasma session`

- Create a new item with the name `LibreOffice Base`
    - `Name`: `LibreOffice Base`
    - `Program`: `lobase`
    - `Icon`: `libreoffice-base`
    - Enable `Enable launch feedback`
    - Disable `Only show when logged into a Plasma session`

### Utilities

- Create a new item with the name `IBus Setup`
    - `Name`: `IBus Setup`
    - `Program`: `ibus-setup`
    - `Icon`: `ibus`
    - Enable `Enable launch feedback`
    - Disable `Only show when logged into a Plasma session`

- Create a new item with the name `KFind`
    - `Name`: `KFind`
    - `Program`: `kfind`
    - `Icon`: `kfind`
    - Enable `Enable launch feedback`
    - Disable `Only show when logged into a Plasma session`

- Create a new item with the name `Sweeper`
    - `Name`: `Sweeper`
    - `Program`: `sweeper`
    - `Icon`: `sweeper`
    - Enable `Enable launch feedback`
    - Disable `Only show when logged into a Plasma session`

- Create a new item with the name `K Color Scheme Editor`
    - `Name`: `K Color Scheme Editor`
    - `Program`: `kcolorschemeeditor`
    - `Icon`: `kcolorchooser`
    - Enable `Enable launch feedback`
    - Disable `Only show when logged into a Plasma session`

- Create a new item with the name `Edit Bookmarks`
    - `Name`: `Edit Bookmarks`
    - `Program`: `keditbookmarks`
    - `Icon`: `bookmark-edit`
    - Enable `Enable launch feedback`
    - Disable `Only show when logged into a Plasma session`

- Create a new item with the name `K Info Center`
    - `Name`: `K Info Center`
    - `Program`: `kinfocenter`
    - `Icon`: `hwinfo`
    - Enable `Enable launch feedback`
    - Disable `Only show when logged into a Plasma session`

- Create a new item with the name `K Font View`
    - `Name`: `K Font View`
    - `Program`: `kfontview`
    - `Icon`: `kfontview`
    - Enable `Enable launch feedback`
    - Disable `Only show when logged into a Plasma session`

- Create a new item with the name `Stacer`
    - `Name`: `Stacer`
    - `Program`: `/usr/bin/stacer`
    - `Icon`: `stacer`
    - Enable `Enable launch feedback`
    - Disable `Only show when logged into a Plasma session`

- Create a new item with the name `Zoom`
    - `Name`: `Zoom`
    - `Program`: `/usr/bin/zoom`
    - `Icon`: `zoom`
    - Enable `Enable launch feedback`
    - Disable `Only show when logged into a Plasma session`

- Create a new item with the name `VirtualBox`
    - `Name`: `VirtualBox`
    - `Program`: `/usr/bin/virtualbox`
    - `Icon`: `virtualbox`
    - Enable `Enable launch feedback`
    - Disable `Only show when logged into a Plasma session`

- Create a new item with the name `Shutter Screen Ruler`
    - `Name`: `Shutter Screen Ruler`
    - `Program`: `/usr/bin/shutter`
    - `Icon`: `kruler`
    - Enable `Enable launch feedback`
    - Disable `Only show when logged into a Plasma session`

- Create a new item with the name `Dolphin`
    - `Name`: `Dolphin`
    - `Program`: `dolphin`
    - `Icon`: `system-file-manager`
    - Enable `Enable launch feedback`
    - Disable `Only show when logged into a Plasma session`

- Create a new item with the name `Krename File Renamer`
    - `Name`: `Krename File Renamer`
    - `Program`: `/usr/bin/krename`
    - `Icon`: `krename`
    - Enable `Enable launch feedback`
    - Disable `Only show when logged into a Plasma session`

- Create a new item with the name `Qdirstat Disk Space Analyzer`
    - `Name`: `Qdirstat Disk Space Analyzer`
    - `Program`: `/usr/bin/qdirstat`
    - `Icon`: `qdirstat`
    - Enable `Enable launch feedback`
    - Disable `Only show when logged into a Plasma session`

- Create a new item with the name `CopyQ`
    - `Name`: `CopyQ`
    - `Program`: `copyq`
    - `Command-Line Arguments`: `show`
    - `Icon`: `copyq`
    - Enable `Enable launch feedback`
    - Disable `Only show when logged into a Plasma session`

### Settings

- Create a new item with the name `System Settings`
    - `Name`: `System Settings`
    - `Program`: `systemsettings`
    - `Icon`: `systemsettings`
    - Enable `Enable launch feedback`
    - Disable `Only show when logged into a Plasma session`

- Create a new item with the name `Hardinfo System Profiler`
    - `Name`: `Hardinfo System Profiler`
    - `Program`: `/usr/bin/hardinfo`
    - `Icon`: `yast-hardware-group`
    - Enable `Enable launch feedback`
    - Disable `Only show when logged into a Plasma session`

### System

- Create a new item with the name `K Help Center`
    - `Name`: `K Help Center`
    - `Program`: `khelpcenter`
    - `Icon`: `system-help`
    - Enable `Enable launch feedback`
    - Disable `Only show when logged into a Plasma session`

- Create a new item with the name `System Monitor`
    - `Name`: `System Monitor`
    - `Program`: `plasma-systemmonitor`
    - `Icon`: `org.kde.plasma.systemmonitor.net`
    - Enable `Enable launch feedback`
    - Disable `Only show when logged into a Plasma session`

- Create a new item with the name `Plasma Discover`
    - `Name`: `Plasma Discover`
    - `Program`: `plasma-discover`
    - `Icon`: `plasmadiscover`
    - Enable `Enable launch feedback`
    - Disable `Only show when logged into a Plasma session`

Save any icons in `/usr/share/icons/hicolor/64x64/apps/`

Save the desktop files in `~/.local/share/applications/`

Run:
```
mkdir ~/.config/microsoft-edge-personal/
cd ~/.local/share/applications/
nano 'Microsoft Edge (Personal).desktop'
```

Paste the following code into this file:
```
[Desktop Entry]
Version=1.0
Type=Application
Name=Microsoft Edge Personal
GenericName=Web Browser
Comment=Microsoft Edge Personal
Exec=/usr/bin/microsoft-edge-stable --user-data-dir="$HOME/.config/microsoft-edge-personal" --class=microsoft-edge-personal
Icon=/usr/share/icons/hicolor/64x64/apps/microsoft-edge-logo-personal.png
Terminal=false
Categories=Network;WebBrowser;
MimeType=text/html;text/xml;application/xhtml+xml;x-scheme-handler/http;x-scheme-handler/https;
StartupWMClass=microsoft-edge-personal
StartupNotify=true
```

Run:
```
chmod +x '/home/abdul/.local/share/applications/Microsoft Edge (Personal).desktop' 
kbuildsycoca5 2>/dev/null || true
```