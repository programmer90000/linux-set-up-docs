# /etc/anacrontab: configuration file for anacron

# See anacron(8) and anacrontab(5) for details.

SHELL=/bin/sh
PATH=/usr/local/sbin:/usr/local/bin:/sbin:/bin:/usr/sbin:/usr/bin
HOME=/root
LOGNAME=root

# Scripts to run

# After how many days to run   How long to wait after startup before running   Unique identifier      Command to run (IN PATH)
7                              0                                               weekly-system-checks   /usr/local/bin/run-commands.sh
